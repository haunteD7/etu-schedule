# Schedule bot

## How to run
Install all requirements:
```sh
pip install -r requirements.txt
```
Create file config.py and write bot's token like that:
```
TOKEN = '<Enter here token>'
```

Then run main.py
```
python main.py
```